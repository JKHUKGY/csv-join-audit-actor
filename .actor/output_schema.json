{
  "actorOutputSchemaVersion": 1,
  "title": "Join audit report",
  "properties": {
    "report": {
      "title": "One JSON audit report",
      "description": "Read verdict and nextAction. A successful Actor run may still report blocked, needs_review, or invalid_input. Only checks_passed means the documented join checks passed; no verdict guarantees correct measurements or aggregates.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    },
    "dataset": {
      "title": "Audit dataset",
      "description": "One report row, with key counts and at most ten examples of each problem type.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
